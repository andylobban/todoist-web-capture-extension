{
  "name": "todoist-web-capture-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "check": "node --check background.js && node --check popup.js && node --check options.js",
    "package": "python3 scripts/package_extension.py"
  }
}
